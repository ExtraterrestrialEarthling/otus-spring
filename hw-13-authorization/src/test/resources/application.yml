spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=LEGACY;
  sql:
    init:
      mode: always
      data-locations: classpath:data-test.sql


  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

logging:
  level:
    ROOT: ERROR